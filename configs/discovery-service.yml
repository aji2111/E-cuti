server:
  port: 8761

spring:
  application:
    name: discovery-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false